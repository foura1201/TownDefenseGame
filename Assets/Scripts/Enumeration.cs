using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enumeration : MonoBehaviour
{
    public enum LaborType { Hammer, Axe, Mine, Farm };
    public enum ResourceType { HP, Coin, Wood, Stone, Food, Time};
    public enum MonsterType { Slime, TurtleShell};
    public enum UISound { Coin, Denied, LaborSpawn, PhaseUp};
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
